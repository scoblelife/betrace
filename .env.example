# BeTrace Environment Configuration

# === Backend (Go) ===
BETRACE_ADDR=:12011
DEMO_MODE=true

# WorkOS (optional — enables JWT auth when set)
# WORKOS_CLIENT_ID=client_xxxxx

# === BFF (Vite/React) ===
VITE_BETRACE_API_URL=http://localhost:12011
VITE_BETRACE_WS_URL=ws://localhost:12011
VITE_DEMO_MODE=true

# WorkOS AuthKit (optional — falls back to demo mode if not set)
# VITE_WORKOS_CLIENT_ID=client_xxxxx

# === Observability (optional) ===
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# VITE_OTEL_ENDPOINT=http://localhost:4318/v1/traces
